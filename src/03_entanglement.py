import torch
import pennylane as qml
import numpy as np

EPOCHS = 10
BATCH_SIZE = 64
LEARNING_RATE = 0.0001
N_QUBITS = 3
SEED = 42
device = torch.device("cpu")

# (Aquí iría la lógica completa de entropía)
print("Script de análisis de entrelazamiento listo.")
